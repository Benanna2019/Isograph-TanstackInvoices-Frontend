
export type Query__CustomerSelect__param = {
  readonly data: {
    readonly customers: ReadonlyArray<{
      readonly id: string,
      readonly name: string,
      readonly email: string,
    }>,
  },
  readonly parameters: Record<PropertyKey, never>,
};
